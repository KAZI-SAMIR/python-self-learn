"""
import random

#random.randarage(1, 3)      # 1, 2 or 3 --> 3
colors = ['red', 'blue', 'yellow']
random.choice(colors)     # choose one color 'red'
random.sample(colors, 2)  # choose two different colors ['yellow', 'red']
random.shuffle(colors)    # mix the color list in-place colors ['yellow', 'red', 'blue']

"""
